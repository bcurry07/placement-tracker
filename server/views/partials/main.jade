.row
    .col-md-3.col-md-offset-2
        button.btn.btn-info(ng-click="addNew()") Add Placement!
hr
.row
    .col-md-8.col-md-offset-2
        table.table.table-striped.table-hover
            thead
                tr
                    td Name
                    td Date
                    td Client
                    td Type
                    td On Billing
                    td Notes
            tbody
                tr(ng-repeat="placement in placements")
                    td {{placement.name}}
                    td {{placement.date | date}}
                    td {{placement.client}}
                    td {{placement.type}}
                    td {{placement.onBilling}}
                    td {{placement.notes}}